
#~ whoami - returns the user
#~ history - which list all the commands we previously used. In the list (!# ex:- !2 to we can run the previous command)
#~ apt = Advanced package tool
#~ apt list -> which list all available packages in the ubuntu
#~ apt update -> which update the package database with latest tools
#~ apt install nano -> which is install nano text editor in ubuntu.
#~ Ctrl + l -> Clear the terminal window


Managing packages

apt update
apt list
apt install nano
apt remove nano